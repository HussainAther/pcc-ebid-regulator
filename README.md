# PCC-EBID Regulator

**Relating Pressure-Control-Chaos (PCC) and Entropy-Based Instability Dynamics (EBID) to classical regulator theorems in cybernetics and control.**

This repository asks a narrow question:

> What changes in classical regulation theory when the instability being regulated is generated by a non-transitive adaptive structure rather than treated only as an external disturbance?

The project begins with three reference points:

1. **Ashby's Law of Requisite Variety** — how much regulatory variety is required?
2. **Conant-Ashby Good Regulator Theorem** — what must a successful regulator model?
3. **Internal Model Principle** — what dynamical structure must be represented internally for robust regulation?

PCC and EBID are treated here as candidate tools for describing the **structure** and **magnitude** of endogenous instability. The goal is comparison, falsification, and computational testing — not to claim that PCC replaces established cybernetics.

## Working hypothesis

In a PCC-like non-transitive system, regulation changes the future distribution of the disturbances it must regulate. Requisite regulator variety may therefore be **state- and trajectory-dependent**, rather than a fixed property of an exogenous disturbance source.

A second hypothesis is that, in strongly cyclic regimes, successful regulators may need an internal representation of **interaction topology and cycle phase**, not merely the current system state.

## Research questions

- Does increasing PCC/EBID instability increase the minimum regulator variety required to maintain bounded error?
- Can EBID predict the empirical requisite-variety threshold?
- Does a regulator with a model of PCC interaction topology outperform a state-only regulator in cyclic regimes?
- Which observations would falsify these proposed connections?

## Repository map

```text
pcc-ebid-regulator/
├── README.md
├── RESEARCH_STATUS.md
├── THEORY_MAP.md
├── theory/
│   ├── requisite_variety.md
│   ├── good_regulator.md
│   └── internal_model_principle.md
├── src/pcc_ebid_regulator/
│   ├── __init__.py
│   ├── dynamics.py
│   ├── drift.py
│   ├── experiments.py
│   ├── metrics.py
│   └── regulators.py
├── experiments/
│   ├── 001_requisite_variety/
│   │   └── run.py
│   ├── 002_good_regulator/
│   │   ├── README.md
│   │   └── run.py
│   └── 003_dynamic_requisite_variety/
│       ├── README.md
│       └── run.py
├── tests/
│   ├── test_drift.py
│   ├── test_metrics.py
│   └── test_regulators.py
└── notes/
    └── legacy_grt_comparison.md
```

## PCC convention used here

The three roles are:

- **Pressure (P):** drives directional change or amplification.
- **Control (C):** constrains or stabilizes pressure.
- **Chaos (Ch):** disrupts or perturbs control.

The canonical cyclic relation is represented as:

`C suppresses P -> P suppresses Ch -> Ch disrupts C`

This repository keeps that convention explicit because changing edge direction changes the mathematical interpretation.

## Minimal experiment

Experiment 001 creates a simple cyclic PCC environment and gives a regulator a finite number of discrete actions. It sweeps regulator variety and estimates the smallest action repertoire that keeps long-run error below a target threshold.

The key empirical object is:

`PCC/EBID instability -> minimum requisite regulator variety`

A monotonic relationship would support further investigation. Failure to find one is also informative.

Run:

```bash
python experiments/001_requisite_variety/run.py
```

Tests:

```bash
python -m pytest -q
```

## Scope and scientific status

This is an exploratory research repository. PCC/EBID are not presented here as established universal laws, and the cybernetic theorems are not reduced to simplified slogans. Claims are separated into:

- established reference theory,
- proposed PCC/EBID interpretation,
- computational hypothesis,
- empirical result.

That separation is intentional.

## Relationship to the PCC repository

This project is a focused satellite of the broader PCC/EBID research program. It isolates regulator-theorem questions so they can be developed and falsified without expanding the scope of the main PCC repository.

## Preliminary baseline findings

The first deterministic baselines are intentionally retained even though they
do **not** yet support the stronger PCC/EBID-regulator hypotheses.

- **Experiment 001:** with the current toy dynamics and an error threshold of
  `0.08`, regulator variety `3` is sufficient at every tested coupling strength
  (`0.5`–`3.0`), while variety `1` fails. Increasing coupling therefore does not
  currently increase the observed threshold.
- **Experiment 002:** the correctly specified one-step PCC predictive model does
  not consistently outperform simpler state-only/history controllers.

- **Experiment 003:** scalar coupling drift modestly degrades performance at
  fixed variety. At a mean-error criterion of `0.030`, stationary random-walk
  controls require variety `5`; stronger stochastic drift sometimes raises the
  empirical threshold to `9`. The effect is threshold-sensitive and not cleanly
  monotonic, so H1 remains unconfirmed.

These null/mixed results are useful. They show that the deterministic symmetric
system and scalar speed drift are not enough to yield a robust variety law. The
next stronger tests are topology switching, component-specific coupling drift,
exogenous disturbance classes, and explicit cycle phase.

See [`RESEARCH_STATUS.md`](RESEARCH_STATUS.md) and
[`HYPOTHESES.md`](HYPOTHESES.md) for claim boundaries and falsification criteria.
